<h1>burguer!</h1>

<h2>Devoured burguers</h2>

<ul>
  {{#each burguer}}
    {{#unless devoured}}
      {{> burguer/burguer-block devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>burguer list!</h2>

<ul>
  {{#each burguer}}
    {{#if devoured}}
      {{> burguer/burguer-block devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a burguer</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">burguer Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="devoured">devoured?</label><br>
    <input type="radio" name="devoured" value="1" checked> devoured!<br>
    <input type="radio" name="devoured" value="0"> Not eaten
  </div>

  <button type="submit">Add burguer</button>
</form>
